name: Briefing Noite (22h BRT)
on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Disparar função
        run: |
          curl -f -X POST \
            -H "x-trigger-secret: ${{ secrets.TRIGGER_SECRET }}" \
            --max-time 300 \
            "https://superb-marzipan-5e98ff.netlify.app/.netlify/functions/briefing-noite"
